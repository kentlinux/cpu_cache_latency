= Cpu Cache Latency =

Программа пытается измерить latency отправки одной кеш линии между разными ядрами одного cpu. 
В результате выводит медианное значение в наносекундах.

== Для сборки необходимо ==
CMake
gcc

== Сборка ==
cd build && cmake .. && make && cd ..

== Запуск ==

build/bin/ccl -h
